# Charakter-Config: Makima (Chainsaw Man)

character:
  name: "makima_(chainsaw_man)"
  booru_tag: "makima_(chainsaw_man)"
  source: "danbooru"

collection:
  # Rating breakdown (2026-07-26): general 4706 (44%), sensitive 4034 (38%),
  # questionable 987 (9%), explicit 1013 (9%) of 10740 total -- only ~18%
  # questionable/explicit, much lower than kitagawa_marin's ~36%.
  ratings: ["general", "sensitive"]
  min_short_side: 768
  exclude_tags: ["photo", "cosplay", "real"]
  max_character_count: 1

captioning:
  leading_tag: "makima_(chainsaw_man)"
  tag_dropout_prob: 0.1

training:
  resolution_area: 1048576 # ~1024x1024
  rank: 16
  alpha: 16
  learning_rate: 1.0e-4
  batch_size: 1
  gradient_accumulation_steps: 4
  weighting_scheme: "logit_normal"
  checkpoint_every_steps: 250
  drive_checkpoint_dir: "/content/drive/MyDrive/charlora/outputs/makima"
