# Charakter-Config: Yor Forger / Yor Briar (Spy x Family)

character:
  name: "yor_briar"
  booru_tag: "yor_briar" # Danbooru-Tag nutzt ihren buergerlichen Namen; "yor_forger" hat 0 Posts
  source: "danbooru"

collection:
  # Rating breakdown (2026-07-26): general 2985 (26%), sensitive 5535 (49%),
  # questionable 1180 (10%), explicit 1580 (14%) of 11280 total -- ~24.5%
  # questionable/explicit.
  ratings: ["general", "sensitive"]
  min_short_side: 768
  exclude_tags: ["photo", "cosplay", "real"]
  max_character_count: 1

captioning:
  leading_tag: "yor_briar"
  tag_dropout_prob: 0.1

training:
  resolution_area: 1048576 # ~1024x1024
  rank: 16
  alpha: 16
  learning_rate: 1.0e-4
  batch_size: 1
  gradient_accumulation_steps: 4
  weighting_scheme: "logit_normal"
  checkpoint_every_steps: 250
  drive_checkpoint_dir: "/content/drive/MyDrive/charlora/outputs/yor_briar"
